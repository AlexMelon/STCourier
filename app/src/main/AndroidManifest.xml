<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.STCourier"
        tools:targetApi="31">

        <activity
            android:name=".sharedactivities.MainActivity"
            android:exported="true"/>

        <activity
            android:name=".sharedactivities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.alex.stcourier.sharedactivities.BookingActivity" />
        <activity android:name="com.alex.stcourier.packagecheck.pres.activity.PackageCheckActivity" />
        <activity android:name="com.alex.stcourier.login.pres.activity.LoginActivity" />

    </application>

</manifest>